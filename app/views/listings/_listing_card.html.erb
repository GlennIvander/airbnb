<%= link_to listing do %>

  <div class="w-full">
    <%= image_tag(listing.images.first, class: "w-[300px] rounded-lg overflow-hidden") if listing.images.any? %>
    <p class="text-lg font-bold"><%= listing.title %></p>
  </div>

<% end %>